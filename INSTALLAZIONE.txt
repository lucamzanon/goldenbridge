╔═══════════════════════════════════════════════════════════════════════════╗
║            TURBOGOLDEN WEB TERMINAL - GUIDA INSTALLAZIONE                ║
╚═══════════════════════════════════════════════════════════════════════════╝

📦 INSTALLAZIONE DIPENDENZE
═══════════════════════════════════════════════════════════════════════════

Il sistema richiede il modulo Python 'websockets'.

Opzione 1 - Con pip (se disponibile):
────────────────────────────────────────
    pip3 install websockets

Opzione 2 - Con apt (Debian/Ubuntu):
────────────────────────────────────────
    apt-get update
    apt-get install python3-pip
    pip3 install websockets

Opzione 3 - Installazione manuale:
────────────────────────────────────────
    wget https://files.pythonhosted.org/packages/source/w/websockets/websockets-12.0.tar.gz
    tar -xzf websockets-12.0.tar.gz
    cd websockets-12.0
    python3 setup.py install

Opzione 4 - Usa il server Flask esistente (NON richiede websockets):
────────────────────────────────────────────────────────────────────
    cd /home/coder/turbogolden-web
    python3 app.py

    Poi apri browser su: http://localhost:8080

═══════════════════════════════════════════════════════════════════════════
✅ VERIFICA INSTALLAZIONE
═══════════════════════════════════════════════════════════════════════════

Esegui:
    python3 -c "import websockets; print('OK')"

Se vedi "OK" → tutto pronto!
Se vedi errore → installa websockets con uno dei metodi sopra

═══════════════════════════════════════════════════════════════════════════
🚀 AVVIO DOPO INSTALLAZIONE
═══════════════════════════════════════════════════════════════════════════

Una volta installate le dipendenze:

    cd /home/coder
    ./start-turbogolden-terminal.sh

═══════════════════════════════════════════════════════════════════════════
